<h2>comments</h2>
    <% @article.comments.each do |comment| %>
    <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
    </p>

    <p><%= comment.created_at %></p>

    <p>
    <strong>Comment:</strong>
    <%= comment.body %>
    </p>

    <p>

    <%= link_to 'Delete Comment', [comment.article, comment],
        method: :Delete,
        data: {confirm: 'Ahhh The comment who lived... Come to die?'}

    %>
    
    </p>

<% end %>